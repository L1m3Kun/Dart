void main() {
  // dart는 객체지향언어로 모든게 객체로 되어있음
  // 모두 class로 선언되어있다
  String name = "nico";
  bool alive = true;
  int age = 12;
  double money = 69.99;
  // num 은 int와 double의 부모 class, 자주 사용되진 않음
  num x = 12;
  x = 1.1;

  // 각각 class이므로 method들을 사용할 수 있음
}
